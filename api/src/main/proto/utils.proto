syntax = "proto3";

import "google/protobuf/wrappers.proto";
import "google/protobuf/empty.proto";

enum Size {
  SMALL = 0;
  MEDIUM = 1;
  LARGE = 2;
}

enum State {
  IDLE = 0;
  BUSY = 1;
  CLOSED = 2;
}